# Test environment variables for MQTT Monitor

NODE_ENV=test
PORT=3500
HOST=0.0.0.0
LOG_LEVEL=error

# Database Configuration (mock in tests)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=iotistic_test
DB_USER=postgres
DB_PASSWORD=postgres
DB_POOL_SIZE=2

# MQTT Broker Configuration (mock in tests)
MQTT_BROKER_URL=mqtt://localhost:1883
MQTT_USERNAME=test_admin
MQTT_PASSWORD=test_password

# Monitor Configuration
MQTT_PERSIST_DB=false
MQTT_DB_SYNC_INTERVAL=10000
MQTT_METRICS_UPDATE_INTERVAL=1000
MQTT_TOPIC_TREE_UPDATE_INTERVAL=1000
