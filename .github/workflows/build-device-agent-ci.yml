name: CI Build, Test & Push Device Agent

on: 
  push:
    branches:
      - master
    paths:
      - 'agent/**' 
      - '.github/workflows/build-device-agent-ci.yml'

jobs:
  test:
    name: Run Unit Tests
    runs-on: ubuntu-24.04
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: agent/package-lock.json

      - name: Install Agent dependencies
        working-directory: ./agent
        run: npm ci

      - name: Run unit tests
        working-directory: ./agent
        run: npm run test:unit

      - name: Build Agent
        working-directory: ./agent
        run: npm run build

  version:
    name: Bump Version
    needs: [test]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    outputs:
      package_version: ${{ steps.bump.outputs.package_version }}
      sha_short: ${{ steps.bump.outputs.sha_short }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Bump version in package.json
        id: bump
        run: |
          cd agent
          # Get current version
          CURRENT_VERSION=$(jq -r '.version' package.json)
          echo "Current version: $CURRENT_VERSION"
          
          # Bump patch version (e.g., 1.0.0 -> 1.0.1)
          IFS='.' read -ra VERSION_PARTS <<< "$CURRENT_VERSION"
          MAJOR="${VERSION_PARTS[0]}"
          MINOR="${VERSION_PARTS[1]}"
          PATCH="${VERSION_PARTS[2]}"
          NEW_PATCH=$((PATCH + 1))
          NEW_VERSION="${MAJOR}.${MINOR}.${NEW_PATCH}"
          
          echo "New version: $NEW_VERSION"
          
          # Update package.json
          jq --arg v "$NEW_VERSION" '.version = $v' package.json > tmp.json && mv tmp.json package.json
          
          # Set outputs
          echo "package_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Commit and push version bump
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add agent/package.json
          git commit -m "chore: bump agent version to ${{ steps.bump.outputs.package_version }} [skip ci]"
          git push origin HEAD

  buildx:
    name: Build & Push to Docker
    needs: version
    runs-on: ubuntu-24.04
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: master

      - name: Display Version Update
        run: |
          echo "## Building Version ${{ needs.version.outputs.package_version }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** \`${{ needs.version.outputs.package_version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Git SHA:** \`${{ needs.version.outputs.sha_short }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Platforms:** \`linux/amd64, linux/arm64, linux/arm/v7\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Docker Images:" >> $GITHUB_STEP_SUMMARY
          echo "- [\`iotistic/agent:latest\`](https://hub.docker.com/r/iotistic/agent/tags?name=latest)" >> $GITHUB_STEP_SUMMARY
          echo "- [\`iotistic/agent:${{ needs.version.outputs.sha_short }}\`](https://hub.docker.com/r/iotistic/agent/tags?name=${{ needs.version.outputs.sha_short }})" >> $GITHUB_STEP_SUMMARY
          echo "- [\`iotistic/agent:${{ needs.version.outputs.package_version }}\`](https://hub.docker.com/r/iotistic/agent/tags?name=${{ needs.version.outputs.package_version }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Installation Scripts:" >> $GITHUB_STEP_SUMMARY
          echo "- [install-docker.sh](https://apps.iotistic.ca/agent/install-docker.sh) ([checksum](https://apps.iotistic.ca/agent/install-docker.sh.sha256))" >> $GITHUB_STEP_SUMMARY
          echo "- [install-systemd.sh](https://apps.iotistic.ca/agent/install-systemd.sh) ([checksum](https://apps.iotistic.ca/agent/install-systemd.sh.sha256))" >> $GITHUB_STEP_SUMMARY
          echo "- [install-docker-${{ needs.version.outputs.package_version }}.sh](https://apps.iotistic.ca/agent/versions/install-docker-${{ needs.version.outputs.package_version }}.sh) ([checksum](https://apps.iotistic.ca/agent/versions/install-docker-${{ needs.version.outputs.package_version }}.sh.sha256))" >> $GITHUB_STEP_SUMMARY
          echo "- [install-systemd-${{ needs.version.outputs.package_version }}.sh](https://apps.iotistic.ca/agent/versions/install-systemd-${{ needs.version.outputs.package_version }}.sh) ([checksum](https://apps.iotistic.ca/agent/versions/install-systemd-${{ needs.version.outputs.package_version }}.sh.sha256))" >> $GITHUB_STEP_SUMMARY

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: buildx-agent-v2-${{ github.sha }}
          restore-keys: |
            buildx-agent-v2-

      # Login to Docker Hub (only if tests passed)
      - name: Login to Docker Hub
        if: success()
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # Build and push to registry
      - name: Build & Push final image
        id: docker_build
        if: success()
        uses: docker/build-push-action@v5
        with:
          context: ./agent
          file: ./agent/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache,mode=max
          tags: |
            iotistic/agent:latest
            iotistic/agent:${{ needs.version.outputs.sha_short }}
            iotistic/agent:${{ needs.version.outputs.package_version }}

  test-agent-docker:
    name: Test Docker Installation Script
    needs: [version, buildx]
    runs-on: ubuntu-24.04
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Make install script executable
        run: chmod +x agent/bin/install-docker.sh

      - name: Run Docker installation script
        env:
          CI: true
          IOTISTIC_AGENT_VERSION: ${{ needs.version.outputs.package_version }}
          IOTISTIC_DEVICE_PORT: 48484
          IOTISTIC_CLOUD_API_ENDPOINT: ''
          IOTISTIC_PROVISIONING_KEY: ''
        run: |
          echo "Testing Docker installation script..."
          sudo -E bash agent/bin/install-docker.sh

      - name: Check agent container is running
        run: |
          if docker ps | grep -q iotistic-agent; then
            echo "✓ Agent container is running"
            docker ps | grep iotistic-agent
          else
            echo "✗ Agent container failed to start"
            docker logs iotistic-agent
            exit 1
          fi

      - name: Test agent API health
        run: |
          echo "Testing agent API endpoints..."
          
          # Try health endpoint (using wget since it's in alpine)
          if docker exec iotistic-agent wget -q -O- http://localhost:48484/health 2>/dev/null || \
             docker exec iotistic-agent wget -q -O- http://localhost:48484/v2/device 2>/dev/null; then
            echo "✓ Agent API is responding"
          else
            echo "⚠ Agent API not responding yet (may still be initializing)"
            docker logs --tail=50 iotistic-agent
          fi

      - name: Verify agent startup
        run: |
          echo "Checking agent logs for successful startup..."
          
          # Check for Device Configuration Summary
          if docker logs iotistic-agent 2>&1 | grep -q "Device Configuration Summary"; then
            echo "✓ Agent started successfully"
          else
            echo "✗ Agent failed to start properly"
            echo "Full logs:"
            docker logs iotistic-agent
            exit 1
          fi

      - name: Show full agent logs
        if: always()
        run: |
          echo "========================================="
          echo "Agent Logs (Full):"
          echo "========================================="
          docker logs iotistic-agent

      - name: Cleanup test environment
        if: always()
        run: |
          docker stop iotistic-agent || true
          docker rm iotistic-agent || true
          docker system prune -f

      - name: Generate test summary
        if: always()
        run: |
          echo "# Docker Installation Script Test" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Script Tested" >> $GITHUB_STEP_SUMMARY
          echo "- **Script**: \`agent/bin/install-docker.sh\`" >> $GITHUB_STEP_SUMMARY
          echo "## Published Image" >> $GITHUB_STEP_SUMMARY
          echo "- **Image**: \`iotistic/agent:${{ needs.version.outputs.package_version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Mode**: Standalone container (local mode)" >> $GITHUB_STEP_SUMMARY
          echo "- **Cloud API**: Not configured (local testing)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Tests Performed" >> $GITHUB_STEP_SUMMARY
          echo "1. Pull published Docker image from registry" >> $GITHUB_STEP_SUMMARY
          echo "2. Container startup" >> $GITHUB_STEP_SUMMARY
          echo "3. API endpoint health check" >> $GITHUB_STEP_SUMMARY
          echo "4. Log verification" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Agent image validated successfully!" >> $GITHUB_STEP_SUMMARY

  test-agent-systemd:
    name: Test Systemd Installation Script
    needs: version
    runs-on: ubuntu-24.04
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Copy agent code to /opt/iotistic/agent (for CI mode)
        run: |
          sudo mkdir -p /opt/iotistic/agent
          sudo cp -r agent/* /opt/iotistic/agent/
          echo "✓ Agent code copied for CI testing"

      - name: Make install script executable
        run: chmod +x agent/bin/install-systemd.sh

      - name: Run systemd installation script
        env:
          CI: true
          IOTISTIC_AGENT_VERSION: ${{ needs.version.outputs.package_version }}
          IOTISTIC_DEVICE_PORT: 48484
          IOTISTIC_CLOUD_API_ENDPOINT: ''
          IOTISTIC_PROVISIONING_KEY: ''
        run: |
          echo "Testing systemd installation script..."
          sudo -E bash agent/bin/install-systemd.sh

      - name: Check systemd service status
        run: |
          echo "Checking systemd service status..."
          sudo systemctl status iotistic-agent --no-pager || true
          
          if sudo systemctl is-active --quiet iotistic-agent; then
            echo "✓ Agent service is running"
          else
            echo "✗ Agent service is not running"
            sudo journalctl -u iotistic-agent -n 50 --no-pager
            exit 1
          fi

      - name: Check PM2 status
        run: |
          echo "PM2 Process List:"
          sudo pm2 list
          
          echo ""
          echo "Agent Process Info:"
          sudo pm2 info iotistic-agent || echo "Process not found"
          
          # Check if process is online
          if sudo pm2 list | grep -q "online"; then
            echo "✓ Agent is running (online)"
          elif sudo pm2 list | grep -q "errored"; then
            echo "✗ Agent is in errored state"
            echo "Checking PM2 logs for errors..."
            sudo pm2 logs iotistic-agent --nostream --lines 100 --err
            exit 1
          elif sudo pm2 list | grep -q "stopped"; then
            echo "✗ Agent is stopped"
            echo "Checking PM2 logs..."
            sudo pm2 logs iotistic-agent --nostream --lines 100
            exit 1
          else
            echo "✗ Agent is not running"
            sudo pm2 logs iotistic-agent --nostream --lines 100
            exit 1
          fi

      - name: Test agent API
        run: |
          echo "Testing agent API..."
          
          if curl -f http://localhost:48484/health 2>/dev/null || \
             curl -f http://localhost:48484/v2/device 2>/dev/null; then
            echo "✓ Agent API is responding"
          else
            echo "⚠ Agent API not responding"
            sudo pm2 logs iotistic-agent --nostream --lines 50
          fi

      - name: Verify agent startup and extract device info
        run: |
          echo "Checking for device configuration in logs..."
          
          # Look for Device Configuration Summary in logs
          if sudo pm2 logs iotistic-agent --nostream --lines 500 | grep -q "Device Configuration Summary"; then
            echo "✓ Agent started successfully - Device configuration found"
            
            # Extract and parse the device configuration JSON
            DEVICE_CONFIG=$(sudo pm2 logs iotistic-agent --nostream --lines 500 | grep "Device Configuration Summary" | tail -1 | sed 's/.*Device Configuration Summary //')
            
            # Pretty print the JSON
            echo ""
            echo "Device Configuration:"
            echo "$DEVICE_CONFIG" | jq '.' || echo "$DEVICE_CONFIG"
            
            # Extract key information for summary
            DEVICE_UUID=$(echo "$DEVICE_CONFIG" | jq -r '.device.uuid' 2>/dev/null || echo "unknown")
            DEVICE_NAME=$(echo "$DEVICE_CONFIG" | jq -r '.device.deviceName' 2>/dev/null || echo "unknown")
            DEVICE_TYPE=$(echo "$DEVICE_CONFIG" | jq -r '.device.deviceType' 2>/dev/null || echo "unknown")
            AGENT_VERSION=$(echo "$DEVICE_CONFIG" | jq -r '.device.agentVersion' 2>/dev/null || echo "unknown")
            PROVISIONED=$(echo "$DEVICE_CONFIG" | jq -r '.device.provisioned' 2>/dev/null || echo "unknown")
            
            # Save to environment for summary step
            echo "DEVICE_UUID=$DEVICE_UUID" >> $GITHUB_ENV
            echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV
            echo "DEVICE_TYPE=$DEVICE_TYPE" >> $GITHUB_ENV
            echo "AGENT_VERSION=$AGENT_VERSION" >> $GITHUB_ENV
            echo "PROVISIONED=$PROVISIONED" >> $GITHUB_ENV
            echo "DEVICE_CONFIG<<EOF" >> $GITHUB_ENV
            echo "$DEVICE_CONFIG" >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV
            
          else
            echo "✗ Agent failed to start - Device configuration not found"
            echo "Full logs:"
            sudo pm2 logs iotistic-agent --nostream --lines 500
            exit 1
          fi

      - name: Show PM2 logs
        if: always()
        run: |
          echo "========================================="
          echo "Agent Logs (PM2):"
          echo "========================================="
          sudo pm2 logs iotistic-agent --nostream --lines 200

      - name: Cleanup
        if: always()
        run: |
          sudo pm2 stop iotistic-agent || true
          sudo pm2 delete iotistic-agent || true

      - name: Generate systemd test summary
        if: always()
        run: |
          echo "# Agent PM2 Process Manager Test" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Installation" >> $GITHUB_STEP_SUMMARY
          echo "- **Location**: \`/opt/iotistic/agent\`" >> $GITHUB_STEP_SUMMARY
          echo "- **User**: \`iotistic\` (non-root)" >> $GITHUB_STEP_SUMMARY
          echo "- **Process Manager**: PM2" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Add device configuration if available
          if [ -n "${{ env.DEVICE_UUID }}" ]; then
            echo "## Device Configuration" >> $GITHUB_STEP_SUMMARY
            echo "- **Device UUID**: \`${{ env.DEVICE_UUID }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- **Device Name**: \`${{ env.DEVICE_NAME }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- **Device Type**: \`${{ env.DEVICE_TYPE }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- **Agent Version**: \`${{ env.AGENT_VERSION }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- **Provisioned**: \`${{ env.PROVISIONED }}\`" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "<details>" >> $GITHUB_STEP_SUMMARY
            echo "<summary>Full Device Configuration (JSON)</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```json' >> $GITHUB_STEP_SUMMARY
            echo '${{ env.DEVICE_CONFIG }}' | jq '.' >> $GITHUB_STEP_SUMMARY || echo '${{ env.DEVICE_CONFIG }}' >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "</details>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "## Tests Performed" >> $GITHUB_STEP_SUMMARY
          echo "1. System dependencies installation (SQLite, Docker, etc.)" >> $GITHUB_STEP_SUMMARY
          echo "2. Agent build and installation" >> $GITHUB_STEP_SUMMARY
          echo "3. PM2 process manager setup" >> $GITHUB_STEP_SUMMARY
          echo "4. Process startup and status check" >> $GITHUB_STEP_SUMMARY
          echo "5. API health verification" >> $GITHUB_STEP_SUMMARY
          echo "6. Auto-reconciliation verification" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Agent running with PM2 validated successfully!" >> $GITHUB_STEP_SUMMARY

  upload-install-scripts:
    name: Upload Installation Scripts
    needs: [version, buildx, test-agent-docker, test-agent-systemd]
    runs-on: ubuntu-24.04
    if: success()
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Enable blob versioning (if not already enabled)
        run: |
          # Check if versioning is already enabled
          VERSIONING_ENABLED=$(az storage account blob-service-properties show \
            --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
            --query 'isVersioningEnabled' -o tsv 2>/dev/null || echo "false")
          
          if [ "$VERSIONING_ENABLED" != "true" ]; then
            echo "Enabling blob versioning on storage account..."
            az storage account blob-service-properties update \
              --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
              --enable-versioning true
            echo "✓ Blob versioning enabled"
          else
            echo "✓ Blob versioning already enabled"
          fi

      - name: Prepare versioned scripts
        run: |
          # Create temporary directory for versioned scripts
          mkdir -p /tmp/iotistic-scripts
          
          # Replace version placeholder in install-docker.sh
          sed "s/AGENT_VERSION_PLACEHOLDER/${{ needs.version.outputs.package_version }}/g" \
            agent/bin/install-docker.sh > /tmp/iotistic-scripts/install-docker.sh
          
          # Replace version placeholder in install-systemd.sh
          sed "s/AGENT_VERSION_PLACEHOLDER/${{ needs.version.outputs.package_version }}/g" \
            agent/bin/install-systemd.sh > /tmp/iotistic-scripts/install-systemd.sh
          
          echo "✓ Prepared scripts with version ${{ needs.version.outputs.package_version }}"

      - name: Generate checksums
        run: |
          cd /tmp/iotistic-scripts
          sha256sum install-docker.sh > install-docker.sh.sha256
          sha256sum install-systemd.sh > install-systemd.sh.sha256
          
          echo "✓ Generated SHA256 checksums:"
          cat install-docker.sh.sha256
          cat install-systemd.sh.sha256

      - name: Upload install-docker.sh (latest)
        run: |
          az storage blob upload \
            --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
            --container-name scripts \
            --name agent/install-docker.sh \
            --file /tmp/iotistic-scripts/install-docker.sh \
            --overwrite true \
            --content-type application/x-sh \
            --content-disposition "attachment; filename=install-docker.sh"
          
          echo "✓ Uploaded agent/install-docker.sh (latest)"

      - name: Upload install-docker.sh checksum (latest)
        run: |
          az storage blob upload \
            --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
            --container-name scripts \
            --name agent/install-docker.sh.sha256 \
            --file /tmp/iotistic-scripts/install-docker.sh.sha256 \
            --overwrite true \
            --content-type text/plain \
            --content-disposition "inline; filename=install-docker.sh.sha256"
          
          echo "✓ Uploaded agent/install-docker.sh.sha256"

      - name: Upload install-docker.sh (versioned)
        run: |
          az storage blob upload \
            --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
            --container-name scripts \
            --name agent/versions/install-docker-${{ needs.version.outputs.package_version }}.sh \
            --file /tmp/iotistic-scripts/install-docker.sh \
            --overwrite true \
            --content-type application/x-sh \
            --content-disposition "attachment; filename=install-docker-${{ needs.version.outputs.package_version }}.sh"
          
          echo "✓ Uploaded agent/versions/install-docker-${{ needs.version.outputs.package_version }}.sh"

      - name: Upload install-docker.sh checksum (versioned)
        run: |
          az storage blob upload \
            --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
            --container-name scripts \
            --name agent/versions/install-docker-${{ needs.version.outputs.package_version }}.sh.sha256 \
            --file /tmp/iotistic-scripts/install-docker.sh.sha256 \
            --overwrite true \
            --content-type text/plain \
            --content-disposition "inline; filename=install-docker-${{ needs.version.outputs.package_version }}.sh.sha256"
          
          echo "✓ Uploaded agent/versions/install-docker-${{ needs.version.outputs.package_version }}.sh.sha256"

      - name: Upload install-systemd.sh (latest)
        run: |
          az storage blob upload \
            --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
            --container-name scripts \
            --name agent/install-systemd.sh \
            --file /tmp/iotistic-scripts/install-systemd.sh \
            --overwrite true \
            --content-type application/x-sh \
            --content-disposition "attachment; filename=install-systemd.sh"
          
          echo "✓ Uploaded agent/install-systemd.sh (latest)"

      - name: Upload install-systemd.sh checksum (latest)
        run: |
          az storage blob upload \
            --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
            --container-name scripts \
            --name agent/install-systemd.sh.sha256 \
            --file /tmp/iotistic-scripts/install-systemd.sh.sha256 \
            --overwrite true \
            --content-type text/plain \
            --content-disposition "inline; filename=install-systemd.sh.sha256"
          
          echo "✓ Uploaded agent/install-systemd.sh.sha256"

      - name: Upload install-systemd.sh (versioned)
        run: |
          az storage blob upload \
            --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
            --container-name scripts \
            --name agent/versions/install-systemd-${{ needs.version.outputs.package_version }}.sh \
            --file /tmp/iotistic-scripts/install-systemd.sh \
            --overwrite true \
            --content-type application/x-sh \
            --content-disposition "attachment; filename=install-systemd-${{ needs.version.outputs.package_version }}.sh"
          
          echo "✓ Uploaded agent/versions/install-systemd-${{ needs.version.outputs.package_version }}.sh"

      - name: Upload install-systemd.sh checksum (versioned)
        run: |
          az storage blob upload \
            --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
            --container-name scripts \
            --name agent/versions/install-systemd-${{ needs.version.outputs.package_version }}.sh.sha256 \
            --file /tmp/iotistic-scripts/install-systemd.sh.sha256 \
            --overwrite true \
            --content-type text/plain \
            --content-disposition "inline; filename=install-systemd-${{ needs.version.outputs.package_version }}.sh.sha256"
          
          echo "✓ Uploaded agent/versions/install-systemd-${{ needs.version.outputs.package_version }}.sh.sha256"

      - name: Upload legacy install.sh (if exists)
        run: |
          if [ -f bin/install.sh ]; then
            az storage blob upload \
              --account-name ${{ secrets.AZURE_STORAGE_ACCOUNT }} \
              --container-name scripts \
              --name install.sh \
              --file bin/install.sh \
              --overwrite true \
              --content-type application/octet-stream \
              --content-disposition "attachment; filename=install.sh"
            
            echo "✓ Uploaded install.sh"
          else
            echo " install.sh not found, skipping"
          fi

      - name: Generate upload summary
        run: |
          echo "# Installation Scripts Uploaded" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Quick Install (Latest)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Docker Installation" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "curl -sSL https://apps.iotistic.ca/agent/install-docker.sh | sudo bash" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Systemd Installation (with PM2)" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "curl -sSL https://apps.iotistic.ca/agent/install-systemd.sh | sudo bash" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Verified Install (with SHA256 checksum)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Docker Installation (Verified)" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "# Download script and checksum" >> $GITHUB_STEP_SUMMARY
          echo "curl -sSL https://apps.iotistic.ca/agent/install-docker.sh -o install-docker.sh" >> $GITHUB_STEP_SUMMARY
          echo "curl -sSL https://apps.iotistic.ca/agent/install-docker.sh.sha256 -o install-docker.sh.sha256" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Verify checksum" >> $GITHUB_STEP_SUMMARY
          echo "sha256sum -c install-docker.sh.sha256" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Run if verification passes" >> $GITHUB_STEP_SUMMARY
          echo "sudo bash install-docker.sh" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Systemd Installation (Verified)" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "# Download script and checksum" >> $GITHUB_STEP_SUMMARY
          echo "curl -sSL https://apps.iotistic.ca/agent/install-systemd.sh -o install-systemd.sh" >> $GITHUB_STEP_SUMMARY
          echo "curl -sSL https://apps.iotistic.ca/agent/install-systemd.sh.sha256 -o install-systemd.sh.sha256" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Verify checksum" >> $GITHUB_STEP_SUMMARY
          echo "sha256sum -c install-systemd.sh.sha256" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Run if verification passes" >> $GITHUB_STEP_SUMMARY
          echo "sudo bash install-systemd.sh" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Version ${{ needs.version.outputs.package_version }} Scripts" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Docker Installation (Versioned)" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "curl -sSL https://apps.iotistic.ca/agent/versions/install-docker-${{ needs.version.outputs.package_version }}.sh | sudo bash" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Systemd Installation (Versioned)" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "curl -sSL https://apps.iotistic.ca/agent/versions/install-systemd-${{ needs.version.outputs.package_version }}.sh | sudo bash" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Environment Variables" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Both scripts support non-interactive installation via environment variables:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- \`IOTISTIC_CLOUD_API_ENDPOINT\`: Cloud API URL (e.g., https://api.iotistic.ca)" >> $GITHUB_STEP_SUMMARY
          echo "- \`IOTISTIC_PROVISIONING_KEY\`: Provisioning API key (leave empty for local mode)" >> $GITHUB_STEP_SUMMARY
          echo "- \`IOTISTIC_DEVICE_PORT\`: Device API port (default: 48484)" >> $GITHUB_STEP_SUMMARY
          echo "- \`IOTISTIC_AGENT_VERSION\`: Specific version to install (default: latest)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Example with cloud connection:**" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "export IOTISTIC_CLOUD_API_ENDPOINT=https://api.iotistic.ca" >> $GITHUB_STEP_SUMMARY
          echo "export IOTISTIC_PROVISIONING_KEY=your-api-key-here" >> $GITHUB_STEP_SUMMARY
          echo "curl -sSL https://apps.iotistic.ca/agent/install-docker.sh | sudo -E bash" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✓ Scripts uploaded to Azure Blob Storage with SHA256 checksums" >> $GITHUB_STEP_SUMMARY

  report:
    name: Generate Reports
    needs: [version, buildx, test-agent-docker, test-agent-systemd, upload-install-scripts]
    runs-on: ubuntu-24.04
    if: always()
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate changelog
        id: changelog
        run: |
          # Get the previous tag
          PREV_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          
          if [ -z "$PREV_TAG" ]; then
            echo "No previous tag found, using all commits"
            COMMITS=$(git log --pretty=format:"- %s (%h)" --no-merges)
          else
            echo "Generating changelog from $PREV_TAG to HEAD"
            COMMITS=$(git log ${PREV_TAG}..HEAD --pretty=format:"- %s (%h)" --no-merges)
          fi
          
          # Save to file
          echo "$COMMITS" > changelog.txt
          
          # Save first 20 lines for summary
          echo "$COMMITS" | head -20 > changelog_summary.txt

      - name: Get Docker image size
        id: image_size
        run: |
          # Login to Docker Hub to pull the image
          echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
          
          # Pull the image
          docker pull iotistic/agent:${{ needs.version.outputs.package_version }}
          
          # Get image size
          SIZE_BYTES=$(docker image inspect iotistic/agent:${{ needs.version.outputs.package_version }} --format='{{.Size}}')
          SIZE_MB=$(echo "scale=2; $SIZE_BYTES / 1024 / 1024" | bc)
          
          echo "size_mb=$SIZE_MB" >> $GITHUB_OUTPUT
          echo "size_bytes=$SIZE_BYTES" >> $GITHUB_OUTPUT
          
          # Get layer count
          LAYERS=$(docker image inspect iotistic/agent:${{ needs.version.outputs.package_version }} --format='{{len .RootFS.Layers}}')
          echo "layers=$LAYERS" >> $GITHUB_OUTPUT

      - name: Generate Summary Report
        run: |
          echo "# Build & Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## Version Information" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** \`${{ needs.version.outputs.package_version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Git SHA:** \`${{ needs.version.outputs.sha_short }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Platform:** \`linux/amd64\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## Docker Image" >> $GITHUB_STEP_SUMMARY
          echo "**Size:** ${{ steps.image_size.outputs.size_mb }} MB" >> $GITHUB_STEP_SUMMARY
          echo "**Layers:** ${{ steps.image_size.outputs.layers }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tags:**" >> $GITHUB_STEP_SUMMARY
          echo "- \`iotistic/agent:latest\`" >> $GITHUB_STEP_SUMMARY
          echo "- \`iotistic/agent:${{ needs.version.outputs.sha_short }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- \`iotistic/agent:${{ needs.version.outputs.package_version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## Changelog" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          cat changelog_summary.txt >> $GITHUB_STEP_SUMMARY
          
          TOTAL_COMMITS=$(wc -l < changelog.txt)
          SHOWN_COMMITS=$(wc -l < changelog_summary.txt)
          if [ $TOTAL_COMMITS -gt $SHOWN_COMMITS ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "_...and $((TOTAL_COMMITS - SHOWN_COMMITS)) more commits_" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## Workflow Status" >> $GITHUB_STEP_SUMMARY
          echo "All jobs completed successfully! 🎉" >> $GITHUB_STEP_SUMMARY

  trigger-integration-tests:
    name: Trigger Integration Tests
    needs: [version, buildx, test-agent-docker, test-agent-systemd, upload-install-scripts]
    runs-on: ubuntu-24.04
    permissions:
      actions: write
      contents: read
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    
    steps:
      - name: Trigger integration tests
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'integration-tests.yml',
              ref: 'master',
              inputs: {
                agent_version: '${{ needs.version.outputs.package_version }}'
              }
            });
            
            console.log('Integration tests triggered with agent version: ${{ needs.version.outputs.package_version }}');
